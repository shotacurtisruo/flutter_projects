import 'package:flutter/material.dart';

void main() {
  //custom function
  runApp(
    const MaterialApp(
      home: Scaffold(
        backgroundColor: Colors.deepPurple,
        body: Center(
          child: Text("Hello, World!"))),
    ),
  ); //functions or executing a function  : this is provided by the flutter framework
  //^ this runs widgets and builds the widget tree
  //scaffold is another widget that provides a structure for the app (white bg)
  //lets add another center widget.

  // eventually make widget trees

  //>formart to organize
  //enforce styling
}



//lets add another scaffold widget